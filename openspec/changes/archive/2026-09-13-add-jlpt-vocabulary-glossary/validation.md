# 验证记录（2026-09-13）

## 自动化验证

`RUN_FFMPEG_TESTS=1 .venv/bin/python -m unittest discover -s tests`：103 项全部通过。

- `test_jlpt_lexicon.py`：词表 schema、版本、哈希、N1–N5、读音归一化、同形异音及显式读音冲突。
- `test_vocabulary.py`：真实 Sudachi 活用/读音归一化、筛选、去重与语境、API 结构校验/重试/拆分/降级、空词表、JSON/Markdown 一致性、原子替换中断和 freshness。
- `test_vocabulary_config.py`：默认值、严格参数类型与范围、启停隔离、预检及密钥错误脱敏。
- `test_vocabulary_pipeline.py`：词汇失败后继续合成与烧录、partial 汇总/退出码、空词表、强制重跑、源字幕改动后双语字幕刷新。
- 既有 CLI、独立烧录、翻译/合成、报告测试全部通过；真实 ffmpeg 测试覆盖单文件、多文件、目录和特殊字符路径。
- 固定提交的 7 个上游文件重新下载并校验哈希，重建的词表、许可证、上游 README 与内置文件逐字节一致。

`git diff --check` 通过。两个 OpenSpec change 的严格校验通过，归档时再次校验。

## 真实课堂样本

从用户授权的 `2026.8.27.mp4` 第 05:00 处取约 45 秒片段，完整运行 `run`：

- 转写 9 条、翻译 9 条、双语字幕 9 条；过滤 1 个疑似幻觉片段。
- 生成 4 个词条：N2 2 个、未分级 2 个；释义降级 0。
- 成片为 H.264 视频，AAC 音频直通；查看抽帧确认日中两行字幕可读。
- 运行报告包含各阶段耗时、词汇数量和全部产物；词表包含数据来源版本，均无 API Key。

人工核对：`形容詞 / けいようし / N2 / 形容词` 与本地词表一致，语境引用第 9 条字幕及对应中文，样本时间 00:00:33.730；`まだ / まだ` 保留为未分级，明确不将词表缺项视为高阶词。另用真实 Sudachi 和内置词表确认 `慌て→慌てる / あわてる / N2`、`見落とし→見落とす / みおとす / N1`。

## 已知范围

词汇提取忠实使用转写结果，不擅自纠正 ASR 拼写或替换读音。在样本中，ASR 将课堂语境的 `名詞` 识成 `名刺`、`ダイエット` 识成 `ダイット`，因此词条会继承这些错误；`名刺` 的 N2/“名片”对应实际转写文本，不能据此认为该课堂讲的是名片。中文字幕在前一处已按上下文译为“名词”。这是原有 ASR 的同音/拼写识别局限，人工校对源字幕后应重新运行流水线；词汇与双语合成会按当前字幕刷新。

JLPT 数据为非官方参考，覆盖范围、许可证、原始来源和多 token 表达限制见 `ja_video_subtitles/data/README.md`。未分级明确包含容易词、专有名词、新词和转写误识；不等同于 N1/N2。
