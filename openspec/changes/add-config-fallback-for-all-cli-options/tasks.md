## 1. 配置解析与校验

- [x] 1.1 按设计映射新增 `[run]`、`[burn]` 选项，保留既有词汇键及默认值；用真实临时 TOML 测试全部映射、旧配置无新增段及配置缺失策略。
- [x] 1.2 实现当前命令作用域的类型、取值和表结构校验；测试布尔字符串/数字、空数组、混合数组、空白/NUL 路径及非表配置，确认错误包含字段名。
- [x] 1.3 实现统一显式值合并和最终必需项校验；逐项测试 CLI > 配置 > 默认、合法但不存在的被覆盖路径不影响运行、非法 CLI 不回退配置、缺少输入/输出时报错。
- [x] 1.4 保留 `run`/`burn` 配置隔离、根目录定位及 cwd 相对路径，增加 `~` 展开；测试不同 cwd、空格/中文路径、burn 忽略非法非相关段和 run 忽略 burn 段。

## 2. 命令行与最终选项接入

- [x] 2.1 放宽位置输入与 `-o` 的 argparse 必填约束，合并后再访问路径；用 CLI 测试确认纯配置 `run`/`burn` 可启动、旧完整命令仍可用、缺少有效输入或输出退出 2 且无预检副作用。
- [x] 2.2 添加 `--no-force`、`--no-yes` 及三态布尔解析；测试未传继承、显式开启/关闭、`-y` 别名与正反开关冲突退出 2。
- [x] 2.3 统一命令入口与预检使用同一次配置加载结果，保留词汇两参数独立覆盖；用集成测试验证主输出、词汇目录、日志/报告与处理函数收到相同有效值，覆盖字段不会在后续被重新加载的配置覆盖。
- [x] 2.4 确认帮助、版本和 `download` 沿用调用控制语义；测试缺失/损坏配置不阻止 `--help`、子命令帮助和 `--version`，download 无新增必需参数。

## 3. 烧录输入、字幕来源与覆盖行为

- [x] 3.1 接入配置输入列表，CLI 非空列表整体替换；测试配置批次、局部 CLI 输入替换、目录排序/去重、空目录及同名冲突保持原有行为。
- [x] 3.2 按来源组优先级解析字幕文件/目录；测试双向 CLI 切换、未覆盖配置双来源报错、单 CLI 来源覆盖配置双来源、配置单字幕用于多视频时报错，以及跟随最终输出目录的默认映射。
- [x] 3.3 将有效 `yes` 与 `force` 接入覆盖确认及产物复用；测试配置开启后自动覆盖、两个否定开关恢复交互、`--no-yes` 与仍开启的 force、`--no-force` 与仍开启的 yes、EOF 跳过且保留已有文件。
- [x] 3.4 验证配置路径经过既有全部字幕校验和输出冲突检查；测试配置 yes/force 不能绕过源视频或字幕保护、符号链接/硬链接冲突，错误退出 2 且不启动编码。

## 4. 使用文档与整体回归

- [x] 4.1 更新 `config.example.toml`，新增命令段、注释路径和默认 false 开关；用 TOML 解析验证模板合法，人工核对映射完整且无需修改真实密钥文件。
- [x] 4.2 更新 CLI 帮助、`README.md`、`README.cn.md`、`README.ja.md`，加入纯配置、局部覆盖、否定开关、字幕来源组及 cwd 示例；逐个核对示例与规范，使用 `run --help` 和 `burn --help` 检查帮助。
- [x] 4.3 执行 `.venv/bin/python -m unittest discover -s tests`，验证现有词汇回退、独立烧录、覆盖确认、日志报告与新 CLI 集成场景全部通过，不调用真实模型或翻译 API。
- [x] 4.4 执行 `openspec validate add-config-fallback-for-all-cli-options --strict --no-interactive` 和 `git diff --check`，确认增量规格与实现改动通过检查，再记录实际验证结果。
